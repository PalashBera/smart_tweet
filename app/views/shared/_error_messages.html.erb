<% if resource.errors.any? %>
  <div class="alert alert-danger">
    <% resource.errors.full_messages.each.with_index(1) do |msg, index| %>
      <p class="mb-0"><%= index %>. <%= msg %>.</p>
    <% end %>
  </div>
<% end %>
